# Secrets
.env

# Python
__pycache__/
*.pyc
*.pyo
.venv/
venv/
*.egg-info/
dist/
build/

# Testing
.pytest_cache/
htmlcov/
.coverage
*.cover
coverage.xml

# Data (user-specific)
data/

# IDE / Tools
.claude/
.vscode/

# Bench — the lookup cache is a local artifact; seeds/prompts/runs are kept
# because they are the evidence behind bench/comparison.md and
# bench/verification.md.
bench/.verify_cache.json
